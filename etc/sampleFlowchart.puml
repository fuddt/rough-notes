@startuml
title フローチャート風: 単純な処理の流れ

start

:開始;
:設定ファイル読込;
if (設定読込OK?) then (Yes)
else (No)
  :エラー終了;
  stop
endif

:入力ファイル読込;
if (入力読込OK?) then (Yes)
else (No)
  :エラー終了;
  stop
endif

:データ検証;
if (検証OK?) then (Yes)
  :データ加工;
  :結果保存;
  :終了;
else (No)
  :不正データとして終了;
endif

stop
@enduml